<%= link_to pet_path(pet) do %>
  <div class="card-pet">
    <% if pet.photos.attached? %>
      <%= cl_image_tag pet.photos.first.key %>
    <% end %>
    <div class="card-pet-infos">
      <div>
        <h2><%= pet.name %></h2>
        <p class="text-dark"><%= pet.address %></p>
      </div>
      <h2 class="text-dark"><%= number_with_precision(pet.price_per_day, precision: 2) %> €</h2>
    </div>
  </div>
<% end %>
