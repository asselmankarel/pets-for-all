<div class="container mb-5">
  <%= link_to icon('fas', 'chevron-left'), pets_path %>
  <div class="d-flex">
    <div class="p-4">
  <% if @pet.photo.attached? %>
    <%= cl_image_tag @pet.photo.key, height: 350, width: 350, crop: :fill, class: "rounded-circle" %>
  <% end %>
    </div>
    <div class="p-4">
      <p>Name: </p>
      <p>Gender: </p>
      <p>Date of birth: </p>
      <p>Address: </p>
      <p>Pet type: </p>
      <p>Price per day: </p>
    </div>
    <div class="p-4">
      <p><%= @pet.name %></p>
      <p><%= @pet.gender %></p>
      <p><%= @pet.birth_date %></p>
      <p><%= @pet.address %></p>
      <p><%= @pet.category %></p>
      <p><%= @pet.price_per_day %></p>
    </div>
  </div>
  <div class="d-flex extra-pics">
    <% 4.times do %>
      <%= cl_image_tag @pet.photo.key, height: 50, width: 50, crop: :fill, class: "rounded-circle m-4" if @pet.photo.attached? %>
    <% end %>
    <%= link_to "Edit", edit_pet_path(@pet), class: "btn m-4" if @pet.user_id == current_user.id %>
    <%= link_to "Remove", pet_path(@pet), {class: "btn m-4", method: :delete, data: {confirm: "Are you sure?"}} if @pet.user_id == current_user.id %>
    <%= link_to "Book now", new_pet_booking_path(@pet), class: "btn m-4" if @pet.user_id != current_user.id %>
  </div>

  <section class="availability mb-4"> 
    <h3>This week availability :</h3>
    <% @available_dates.each do |date| %>
      <% if date[:available] %>
        <span class="badge badge-pill badge-success" title='<%= "#{date[:date].day}/#{date[:date].month}" %>'><%= date[:date].day %></span>
      <% else %>
        <span class="badge badge-pill badge-danger" title='<%= "#{date[:date].day}/#{date[:date].month}" %>'><%= date[:date].day %></span>
      <% end %>
    <% end %>
  </section>
  <section id="about-pet">
    <h4>About <%= @pet.name %> :</h4>
    <p><%= @pet.description %></p>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Nesciunt rerum sint earum consequatur quia? Quasi perferendis praesentium consequuntur asperiores quis, voluptatibus excepturi dolorum ut, repellendus earum, tempore aliquid omnis minima est perspiciatis libero laborum molestias fugiat? Nisi, quo? Blanditiis ipsum optio nam fugiat quas. Eius voluptates laboriosam dolorum tempore omnis?</p>
  </section>
</div>
<section id="other-pet" class="row-full py-4">
  <div class="container">
    <h2>You may also want to rent these pets :</h2>
    <div class="d-flex justify-content-around">
      <% 3.times do %>
        <% category_sample = Pet::CATEGORIES.sample %>
        <%= link_to "/pets/find/#{category_sample}" do %>
          <div class="card-category m-3" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path(category_sample) %>')">
            <p><%= category_sample %></p>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>
