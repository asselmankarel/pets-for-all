<%= simple_form_for [@pet, @review] do |f| %>
  <%= f.input :rating, collection: (1..5).to_a %>
  <%= f.input :content %>
  <%= button_tag :submit, class: "btn btn-primary w-100" do %>
    <%= icon('fas', 'check', "Add a review") %>
  <% end %>
<% end %>