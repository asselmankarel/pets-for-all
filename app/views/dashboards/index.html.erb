<div class="container">
  <div class="row mt-3">
    <div class="col">
    <h3>Incomming bookings</h3>

    </div>

    <div class="col">
      <h3>My pets</h3>

      <ul class="list-unstyled">
        <% @pets.each do |pet| %>
          <li class="list-unstyled-item"><%= link_to  'edit', edit_pet_path(pet) %> | <%= pet.name %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

